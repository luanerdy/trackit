import React from 'react';
import StyledTitle from './styles';

const Title = ({text}) => {
    return (
        <StyledTitle>
            {text}
        </StyledTitle>
    );
}

export default Title;
